# Layer 3 Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                         CLIENT (Frontend)                           │
│                    (Layer 4 - To be implemented)                    │
└────────────────────────────────┬────────────────────────────────────┘
                                 │ HTTP Requests
                                 │
┌────────────────────────────────┴────────────────────────────────────┐
│                        EXPRESS.JS SERVER                            │
│                      (Layer 2 - Complete)                           │
├─────────────────────────────────────────────────────────────────────┤
│  Routes:                                                            │
│  ├─ /api/auth          (Authentication)                            │
│  ├─ /api/users         (User Management)                           │
│  ├─ /api/courses       (Course Management)                         │
│  ├─ /api/questions     (Question Bank)                             │
│  ├─ /api/assessments   (Assessment Management)                     │
│  ├─ /api/results       (Results & Grading)                         │
│  ├─ /api/analytics     (Performance Analytics)                     │
│  └─ /api/ai ⭐         (AI Intelligence - NEW!)                     │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                    ┌────────────┴────────────┐
                    │                         │
         ┌──────────┴──────────┐   ┌─────────┴──────────┐
         │   Layer 3: AI       │   │    Layer 1:        │
         │ Intelligence Layer  │   │ Database Layer     │
         └──────────┬──────────┘   └─────────┬──────────┘
                    │                         │
    ┌───────────────┼───────────────┐        │
    │               │               │         │
┌───┴───┐      ┌────┴────┐    ┌────┴────┐   │
│Ollama │      │Adaptive │    │Context  │   │
│Service│      │Assessment│   │Update   │   │
└───┬───┘      │Service  │    │Service  │   │
    │          └────┬────┘    └────┬────┘   │
    │               │              │         │
┌───┴────────┐     │              │    ┌────┴─────┐
│Question    │     │              │    │ MongoDB  │
│Generator   │─────┘              └────│ Database │
│Service     │                         └──────────┘
└────────────┘


┌─────────────────────────────────────────────────────────────────────┐
│                      OLLAMA (Local LLM)                             │
│                   Running on localhost:11434                        │
├─────────────────────────────────────────────────────────────────────┤
│  Models Available:                                                  │
│  ├─ llama2 (3.8GB)    - General purpose                           │
│  ├─ mistral (4.1GB)   - Fast and efficient                        │
│  └─ phi (1.6GB)       - Lightweight                               │
└─────────────────────────────────────────────────────────────────────┘
```

## Data Flow Diagrams

### 1. Question Generation Flow
```
┌──────────┐
│ Faculty  │
│ Request  │
└────┬─────┘
     │
     │ POST /api/ai/generate-questions
     │ { topic, difficulty, count, type }
     │
     v
┌────────────────┐
│  AI Controller │
│   (aiController)│
└────┬───────────┘
     │
     │ Call generateQuestions()
     │
     v
┌────────────────────┐
│ Question Generator │
│     Service        │
└────┬───────────────┘
     │
     ├─ buildPrompt()
     │  (Create structured prompt)
     │
     v
┌────────────────┐
│ Ollama Service │
│  (Local LLM)   │
└────┬───────────┘
     │
     │ Generate text
     │
     v
┌────────────────────┐
│ Question Generator │
│     Service        │
└────┬───────────────┘
     │
     ├─ parseGeneratedQuestions()
     │  (Parse AI output)
     │
     ├─ Save to MongoDB
     │
     v
┌──────────────┐
│   MongoDB    │
│ (Questions)  │
└──────────────┘
```

### 2. Adaptive Assessment Flow
```
┌──────────┐
│ Student  │
│ Request  │
└────┬─────┘
     │
     │ POST /api/assessments/generate
     │ { courseId, totalQuestions }
     │
     v
┌────────────────────┐
│Assessment Controller│
└────┬───────────────┘
     │
     │ Call generateAdaptiveAssessment()
     │
     v
┌──────────────────────┐
│  Adaptive Assessment │
│       Service        │
└────┬─────────────────┘
     │
     ├─ analyzeStudentPerformance()
     │  ├─ Get past results
     │  ├─ Calculate topic accuracy
     │  ├─ Identify weak areas
     │  └─ Determine optimal difficulty
     │
     ├─ calculateTopicPriorities()
     │  ├─ Weak topics: weight 3
     │  ├─ Average topics: weight 2
     │  └─ Strong topics: weight 1
     │
     ├─ selectAdaptiveQuestions()
     │  ├─ Fetch from high-priority topics
     │  ├─ Apply difficulty distribution
     │  └─ Randomize order
     │
     v
┌──────────────┐
│  Assessment  │
│   Created    │
└──────────────┘
```

### 3. Context Update Flow
```
┌──────────┐
│ Student  │
│ Submits  │
│   Test   │
└────┬─────┘
     │
     │ POST /api/assessments/:id/submit
     │ { answers, resultId }
     │
     v
┌────────────────────┐
│Assessment Controller│
│  submitAssessment() │
└────┬───────────────┘
     │
     ├─ Grade answers
     ├─ Calculate scores
     ├─ Calculate topic performance
     │
     v
┌────────────────┐
│ Result Saved   │
└────┬───────────┘
     │
     │ Auto-trigger
     │
     v
┌──────────────────────┐
│ Context Update       │
│     Service          │
└────┬─────────────────┘
     │
     ├─ updateStudentContext()
     │  ├─ Calculate weighted averages
     │  ├─ Update topic scores
     │  └─ Update learning metrics
     │
     ├─ Identify changes
     │  ├─ Topics improved
     │  ├─ Topics declined
     │  └─ Speed changes
     │
     v
┌──────────────┐
│ User Context │
│   Updated    │
└──────────────┘
```

### 4. Learning Insights Flow
```
┌──────────┐
│ Student/ │
│ Faculty  │
│ Request  │
└────┬─────┘
     │
     │ GET /api/ai/insights/:studentId
     │
     v
┌────────────────┐
│  AI Controller │
└────┬───────────┘
     │
     │ Call getLearningInsights()
     │
     v
┌──────────────────────┐
│ Context Update       │
│     Service          │
└────┬─────────────────┘
     │
     ├─ Get student context
     ├─ Get recent results (10)
     │
     ├─ Identify strengths (>75%)
     ├─ Identify weaknesses (<50%)
     │
     ├─ analyzeLearningStyle()
     │  ├─ Pace (fast/moderate/slow)
     │  ├─ Consistency
     │  └─ Adaptability
     │
     ├─ analyzeProgressTrend()
     │  ├─ Compare recent vs older
     │  └─ Calculate improvement %
     │
     ├─ calculateMasteryLevel()
     │  └─ Overall proficiency
     │
     ├─ generateRecommendations()
     │  ├─ Focus areas
     │  ├─ Speed improvement
     │  ├─ Consistency tips
     │  └─ Positive reinforcement
     │
     v
┌──────────────┐
│   Insights   │
│   Response   │
└──────────────┘
```

## Service Dependencies

```
┌──────────────────────┐
│  ollamaService.js    │ ← Core LLM integration
└──────────┬───────────┘
           │
           ├──────────────────┐
           │                  │
┌──────────┴──────────┐  ┌───┴──────────────┐
│questionGenerator    │  │  AI Controller   │
│    Service          │  │  (aiController)  │
└──────────┬──────────┘  └──────────────────┘
           │
           └─────────────┐
                         │
┌────────────────────────┴──────────────┐
│   adaptiveAssessmentService.js        │
│   - Uses Question model                │
│   - Uses Result model                  │
│   - Uses User model                    │
└────────────┬──────────────────────────┘
             │
             └─────────────┐
                           │
┌──────────────────────────┴──────┐
│   contextUpdateService.js       │
│   - Uses User model             │
│   - Uses Result model           │
└─────────────────────────────────┘
```

## API Endpoint Map

```
/api/ai/
├── GET    /status                          [Faculty, Admin]
│   └─ Check Ollama service and models
│
├── POST   /generate-questions              [Faculty, Admin]
│   ├─ topic (required)
│   ├─ difficulty (easy/medium/hard)
│   ├─ count (1-10)
│   ├─ questionType (multiple-choice/true-false/short-answer)
│   ├─ course (required)
│   ├─ context (optional)
│   └─ saveToDatabase (true/false)
│
├── POST   /generate-for-weak-topics        [Faculty, Admin]
│   ├─ studentId (required)
│   └─ courseId (required)
│
├── POST   /enhance-question/:id            [Faculty, Admin]
│   └─ Improves existing question
│
├── GET    /insights/:studentId             [Student (self), Faculty, Admin]
│   └─ Returns learning insights
│
├── POST   /chat                            [All authenticated]
│   ├─ message (required)
│   └─ context (array of previous messages)
│
└── POST   /pull-model                      [Admin]
    └─ modelName (required)
```

## Tech Stack Overview

```
┌─────────────────────────────────────────┐
│         Technology Stack                │
├─────────────────────────────────────────┤
│ Runtime:      Node.js v18+              │
│ Framework:    Express.js                │
│ Database:     MongoDB + Mongoose        │
│ Auth:         JWT (jsonwebtoken)        │
│ AI:           Ollama (Local LLM)        │
│ Models:       llama2, mistral, phi      │
│ Testing:      Custom test suite         │
│ Docs:         Markdown + JSDoc          │
└─────────────────────────────────────────┘
```

---

## Legend
```
⭐ = New in Layer 3
✅ = Complete
📋 = Planned
🔧 = Modified
✨ = Created
```
